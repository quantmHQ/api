syntax = "proto3";

package ctrlplane.common.v1;

enum AuthProvider {
  AUTH_PROVIDER_UNSPECIFIED = 0;
  AUTH_PROVIDER_GOOGLE = 1;
  AUTH_PROVIDER_GITHUB = 2;
}

enum RepoProvider {
  REPO_PROVIDER_UNSPECIFIED = 0;
  REPO_PROVIDER_GITHUB = 1;
  REPO_PROVIDER_GITLAB = 2;
}
